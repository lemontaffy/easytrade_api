<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="com.cake.easytrade.mapper.PermissionMapper">
    <select id="findById" parameterType="java.lang.Long" resultType="com.cake.easytrade.model.Permission">
        SELECT * FROM user_roles WHERE user_id = #{id}
    </select>
</mapper>